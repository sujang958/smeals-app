import { StyleSheet } from "react-native"

const OSSStyles = StyleSheet.create({
  OSSItemContainer: {
    paddingVertical: 10,
  },
  OSSItemTitle: {
    fontSize: 20,
  },
  OSSItemVersion: {
    fontSize: 16,
  },
  OSSItemLicenseContainer: {
    flexDirection: "row",
    padding: 6,
    backgroundColor: "#111",
    marginTop: 6,
  },
})

export default OSSStyles
